{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for the e-commerce admin platform.",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
